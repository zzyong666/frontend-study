import { AppClickDirective } from './app-click.directive';

describe('AppClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AppClickDirective();
    expect(directive).toBeTruthy();
  });
});

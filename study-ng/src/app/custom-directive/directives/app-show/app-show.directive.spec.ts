import { AppShowDirective } from './app-show.directive';

describe('AppShowDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShowDirective();
    expect(directive).toBeTruthy();
  });
});

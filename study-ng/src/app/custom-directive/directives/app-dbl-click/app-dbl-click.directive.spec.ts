import { AppDblClickDirective } from './app-dbl-click.directive';

describe('AppDblClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDblClickDirective();
    expect(directive).toBeTruthy();
  });
});

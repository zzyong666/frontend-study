import { AppHiddenDirective } from './app-hidden.directive';

describe('AppHiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
